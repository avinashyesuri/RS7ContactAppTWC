import React from 'react'
import Modal from './Modal';
const AddUpdateCard = ({isOpen, onClose}) => {
  return (
    <div>


<Modal  isOpen={isOpen} onClose={onClose}>
  Hi how are u
</Modal>
    </div>
  )
}

export default AddUpdateCard